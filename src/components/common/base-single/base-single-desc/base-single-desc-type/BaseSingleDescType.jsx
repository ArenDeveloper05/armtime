const BaseSingleDescType = ({ type }) => {
  return (
    <div className="base-single-desc-type">
      <span className="base-single-desc-type-txt1">{type}</span>
    </div>
  );
};

export default BaseSingleDescType;
