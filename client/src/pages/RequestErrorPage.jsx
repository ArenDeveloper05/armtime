import RequestError from "../components/errors/requestError/RequestError";
import Layout from "../layout/Layout";

const RequestErrorPage = () => {
  return (
    <Layout>
      <RequestError />
    </Layout>
  );
};

export default RequestErrorPage;
